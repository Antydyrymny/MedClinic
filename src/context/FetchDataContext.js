import { createContext } from 'react';

export const DoctorsAllContext = createContext(null);
export const SpecialitiesContext = createContext(null);
export const ClinicsContext = createContext(null);
